Requirements: Python3.7, MySql

Run command ``pip install -r requirements.txt``

Set your db settings in db.py

Run command ``python db.py`` for migrate tables in your db.

Start app ``python entry.py``